
```java
Node ref = sentinel.next;
Node checker; 
while (checker.next != null) {
    checker = checker.next; 
    while () {
        if (checker.contains(ref.next)) {
            
        }
            }
    ref = sentinel.next;
} 
```

For Linked List questions: 
1. start with most information 
2. draw it out (i.e. start from inside and write code)
      